{
  "name": "ctw-main",
  "version": "1.0.0",
  "description": "DormScout is a Georgia-based student housing and roommate matching platform created by DoubleRLD.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}